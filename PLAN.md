# اکسسوری آس — پلن کامل (مطابق فیگما، سپتامبر ۲۰۲۶)

> Figma: `accesssory as` کلید `4RqKMjYhwQ5FALfZdEbg6V` آخرین تغییر `2026-09-12`. حدود ۵۰ فریم دسکتاپ + ۲۵ فریم موبایل در پیج `↪︎ Design`. پیج‌های Design System خالی‌اند. DB: MySQL لوکال. پرداخت: زرین‌پال + زیبال.

## ۰. تصمیم‌های استک

- **Next 16.3.5 + React 19.2 + TS + App Router + `src/` + Tailwind v4 + ESLint 9 + Turbopack** — Node ≥20.9.
- **ORM: Drizzle** code-first، مایگریشن SQL خوانا، MySQL فرست‌کلاس.
- **Auth: Better Auth** سشن دیتابیسی + پلاگین `phoneNumber` و `emailOTP` برای سناریوی موبایل/OTP فیگما.
- **اعتبارسنجی: Zod + React Hook Form + resolvers** — یک اسکیما کلاینت+سرور.
- **استیت: TanStack Query (سروراستیت) + Zustand (سبد، علاقه‌مندی خوش‌بینانه، UI شیت/مودال)** — دیتای API داخل Zustand نه.
- **UI: shadcn/ui + Radix + CVA + lucide-react** — RTL دستی.
- **پرداخت: `zarinpal-node-sdk` رسمی + Zibal با REST مستقیم** (`/v1/request` و `/v1/verify`). تومان در UI/DB، ریال (×۱۰) فقط لحظه پرداخت.
- **تست: Vitest** (fa/قیمت/کوپن/جمع سبد) + Playwright بعداً برای چک‌اوت و لاگین.

## ۱. موجودی فیگما (source of truth)

| گروه | فریم‌ها (node-id) | سایز | وضعیت کد |
|---|---|---|---|
| لندینگ Main | `1:2`، `612:135`، `76:13`، `418:40`، `1049:423` | 1440×3914 / 1440×1718 / 1440×1024 | نشده |
| فروشگاه Shopping | `627:3627` | 1440×2447 | شده (پایه) |
| هاور سبد | `749:332` | 1440×2456 | نشده |
| مگامنو | `6299:4271` | 1440×1024 | نشده |
| محصول: پایه/جزئیات/ریویو/ثبت‌نظر | `1248:512` (2767) / `1758:530` (2200) / `1926:567` (2376) / `2615:1248` (مودال) | — | پایه شده، مودال نشده |
| چک‌اوت سبد | `776:299` | 1440×1411 | نشده |
| چک‌اوت آدرس بار اول | `812:325` | 1440×1593 | نشده |
| چک‌اوت آدرس‌های ذخیره‌شده | `872:374` | 1440×1593 | نشده |
| چک‌اوت ارسال-پرداخت | `941:6997` | 1440×1667 | نشده |
| پرداخت موفق/ناموفق | `1087:509` / `1113:504` | 1440×1614 | نشده |
| لاگین ۸ حالت | `1116:512`، `1154:457`، `1168:458`، `2742:2275`، `2854:1308`، `2857:2287`، `2860:3265`، `2862:4271` | 1440×1024 | نشده |
| حساب: داشبورد/سفارش‌ها/جزئیات ×۲/علاقه‌مندی/آدرس ×۴/اطلاعات + ۱۱ مودال | `1999:1384`، `2116:694`، `3279:7236`، `3832:1790`، `2124:1141`، `2176:681`، `2365:767`، `2345:1201`، `2116:5433`، `2389:8657`، `2460:834`، `2479:2194`، `2480:942`، `2487:2194`، `2537:3682`، `2490:3464`، `2493:1038`، `2493:2303`، `2499:3532`، `2509:4814`، `2521:1156`، `2527:2414` | 1440×1007 تا 1287 | نشده |
| تماس/سوالات/درباره | `2895:5078` (1844) / `3279:4465` (2041) / `3277:1593` (1338) | — | نشده |
| موبایل ~۲۵ فریم | `3897:2176` به بعد (شاپ، سورت/فیلتر شیت، محصول، ریویو + مودال سبد، چک‌اوت، اکانت) | 390-430 عرض | نشده |

## ۲. توکن‌ها و قراردادهای بصری

- رنگ‌ها: `--color-brand:#0A5954` (CTA/لینک فعال)، `brand-deep:#00201D`، `wine:#9E1238` (تخفیف/خطا)، `sand:#D6C2A1`، `mist:#F7FAFA` (بک‌گراند صفحه)، `ink:#111C21`، `muted-fg:#4A5463`. همان `@theme inline` فعلی.
- فونت: **Doran FaNum ONLY**، هیچ فالبک مجاز نیست (رول کاربر). اعداد نمایشی فارسی via `fa.ts`؛ محاسبات number تومان.
- گوشه‌ها: کارت محصول `rounded-2xl`، دکمه/بج `rounded-full` برای تخفیف، باکس‌های چک‌اوت/اکانت `rounded-2xl border bg-white`.
- زبان `fa` + `dir=rtl`. بریدکرامب جداکننده `/`.

## ۳. شل سراسری (همه صفحات فروشگاهی)

- **Top-Nav** (`Top - Nav`): راست لوگو `AS accessory` + «اکسسوری آس / روایتی از سلیقه تو»، وسط «دسته‌بندی محصولات، فروشگاه، تماس با ما، درباره ما»، چپ «ورود یا ثبت نام / حساب کاربری» + بج سبد `۵`. دسکتاپ `h-22`؛ موبایل ناو افقی اسکرول. `دسته‌بندی محصولات` → مگامنو هاور (`6299:4271`)؛ آیکون سبد → پاپ‌اور سبد (`749:332`).
- **Footer** (همه فریم‌ها یکسان): فیچر ۴تایی (تحویل اکسپرس / حمل رایگان بالای ۵۰۰هزار / ضمانت ۷روزه / کیفیت پرمیوم) + ستون‌های «راهنمای خرید، خدمات مشتریان، با اکسسوری آس» + آدرس ولیعصر/زرتشت/جاوید پلاک ۲۴ + `۰۲۱ ۷۰۰۸۰۰۱ ــ ۰۹۳۵ ۱۷۹ ۰۸۵۳` + پاراگراف «روایتی از سلیقه شما...» + کپی‌رایت. کامپوننت `Footer` فعلی کافی، فقط متن بلند درباره به `about` لینک شود.
- **بریدکرامب**: شاپ «اکسسوری آس / فروشگاه»، محصول «... / انگشتر / صفحه محصول»، چک‌اوت «... / سبد خرید [/ آدرس شما / روش ارسال]»، اکانت «... / پروفایل کاربری / داشبورد».

## ۴. مشخصات صفحه‌به‌صفحه

### ۴.۱ لندینگ `1:2` / `612:135` (نشده — بعد از چک‌اوت)

- هیرو `76:13`: تیتر «اکسسوری آس / انتخابی برای خاص‌پسندان» + ساب «اکسسوری‌های خاص و ماندگار برای تکمیل استایل روزمره و رسمی شما».
- بنر فروش ویژه: «٪۷۵ تخفیف به مناسبت روز دختر / اکسسوری‌هایی برای امروز و سال‌های بعد / فروش ویژه!».
- سکشن‌ها به ترتیب `1:2`: هیرو → `New Products` → `Product List` → `Offers` + `Banner` → فوتر. `Offers` همان کاروسل شاپ، `revalidate:60`.
- پیاده‌سازی: `app/page.tsx` فعلی شاپ است؛ لندینگ می‌شود `/` جدید و شاپ می‌رود `/shop` با ریدایرکت `/`؟ نه — تصمیم کم‌هزینه: `/` لندینگ، `/shop` لیست فعلی (بریکرامب و لینک‌های فوتر/ناو به `/shop` آپدیت). `ponytail:` تا قبل چک‌اوت لندینگ نساز، شاپ روی `/` بماند.

### ۴.۲ فروشگاه `627:3627` (شده، نیاز به پولیش)

- بریدکرامب + گرید `offers` ۵ کارت (انگشتر فول نگین، ۴٬۲۵۰٬۰۰۰، بج ٪۲۰) + باکس «پیشنهاد شگفت‌انگیز» با شمارش معکوس `۰۳ روز : ۲۰ ساعت : ۳۵ دقیقه` + «مشاهده همه».
- `SortBar`: «۳۵۷ اکسسوری» (total واقعی) + «مرتب‌سازی: همه / جدیدترین / ارزان‌ترین / پرفروش‌ترین / گران‌ترین» via `?sort=`.
- `FiltersSidebar` ۳۳۰px: «فیلترها / حذف فیلترها»، «فقط کالاهای موجود»، «محدوده قیمت ۲۵۰٬۰۰۰ - ۲۵٬۰۵۰٬۰۰۰ تومان» (اسلایدر دوطرفه)، «محصولات» ۷ دسته (گردنبند/انگشتر/دستبند/گوشواره/پابند/نیم‌ست/ست کامل)، «رنگ» ۶تایی (طلایی/صورتی/نقره‌ای/بنفش/آبی/سفید)، «سایز» ۲۰-۲۴(+۲۵ کد). همه via URL searchParams.
- کارت: تصویر مربع، بج تخفیف، تایتل، قیمت + خط‌خورده، دکمه سبد، «ناموجود».
- پولیش مانده: شمارش معکوس واقعی offers، اسلایدر رنج (فعلاً input عددی است)، سورت `popular` (فعلاً createdAt؛ نیاز به `sold_count` یا فallback).

### ۴.۳ هاور سبد `749:332` + مگامنو `6299:4271` (نشده — همراه صفحه سبد)

- پاپ‌اور سبد روی هاور/کلیک آیکون: لیست ۳-۴ خط (عکس، تایتل، تعداد، قیمت)، جمع، CTA «مشاهده سبد / ثبت سفارش». بستن با Esc/کلیک بیرون، فوکوس‌تراپ سبک.
- مگامنو: پنل تمام‌عرض زیر ناو با ۷ دسته + چند محصول منتخب هر دسته. دیتا از `getFilterMeta` + `getOffers`، کش ساعتی.

### ۴.۴ محصول — ۴ فریم + مودال (پایه شده)

- مشترک همه: گالری مربع + ۴ تامب، دسته «انگشتر»، تایتل «انگشتر فول نگین زنانه WC۰۰۹» (sku داخل تایتل)، امتیاز `(میانگین امتیاز کاربران) ۴.۶ / ۴۸۳ دیدگاه`، انتخاب رنگ/سایز، باکس دوقلو «افزودن به سبد خرید / اضافه به سبد خرید» (اصلی + secondary)، باکس فیچر ۴تایی داخل ستون خرید، تب «مشخصات محصول (۳) / دیدگاه کاربران»، «کالاهای مشابه» ۴-۵ کارت، باکس امتیاز aggregate.
- `1758:530` جزئیات: تب مشخصات فعال — جدول جنس/کشور/کد/دسته.
- `1926:567` ریویو: لیست کامل + «۴۸۳ دیدگاه دیگر» (پجینیشن/«بیشتر»).
- `2615:1248` مودال ثبت دیدگاه: اورلی + کارت «ثبت دیدگاه / نظر خود را... / [نظر شما...] / ثبت دیدگاه» — نیاز به لاگین؛ بعد ثبت revalidate تگ محصول.
- آیتم ریویو: نام (حسین کاظمی)، امتیاز ۵.۰، تاریخ `۱۴۰۵/۰۲/۲۵`، متن. فرم: امتیاز ستاره‌ای + متن (Zod، Server Action، rate-limit ساده).
- پیاده‌سازی: `products/[slug]/page.tsx` فعلی + `?tab=specs|reviews`، کامپوننت‌های `Gallery` (client، انتخاب تامب)، `RatingBadge`، `ReviewList`، `ReviewForm` (موجود، اتصال به اکشن واقعی).

### ۴.۵ چک‌اوت — ۶ قدم (نشده — P2، مهم‌ترین گپ)

شل مشترک `app/checkout/layout.tsx`: استپر ۴ مرحله‌ای (سبد → آدرس → ارسال → پرداخت) + ستون «جزئیات پرداخت» (مجموع کالاها (N کالا) / سود شما / مجموع سبد + هشدار «مبلغ هنوز پرداخت نشده و در صورت اتمام موجودی کالاها حذف می‌شوند») + فوتر.

1. `cart` صفحه `776:299` (`/cart` یا `/checkout`): خطوط سبد (عکس/تایتل/سایز/تعداد stepper/قیمت/حذف)، خالی‌بودن (CTA فروشگاه)، CTA «ثبت سفارش» → آدرس. سبد مهمان در Zustand + persist؛ بعد لاگین مرج سمت سرور.
2. `address-first` صفحه `812:325` (`/checkout/address`): فرم آدرس کامل (گیرنده/موبایل/استان/شهر/متن/کدپستی/موقعیت روی نقشه `ponytail:` لینک دستی + دکمه «ثبت دستی آدرس») + «تایید و ادامه» + «برگشت به مرحله قبلی» + باکس «جزئیات مرسوله».
3. `address-saved` صفحه `872:374` (همان روت، حالت دیتادار): «انتخاب آدرس ارسال» رادیوکارتی + «افزودن آدرس جدید» + ویرایش/حذف.
4. `shipping` صفحه `941:6997` (`/checkout/shipping`): «انتخاب روش ارسال» (پست پیشتاز/تیپاکس با هزینه، رایگان بالای ۵۰۰هزار) + باکس کد تخفیف (input + «ثبت»، پیام موفقیت «کد تخفیف با موفقیت اعمال شد! GH632LO») + «تایید و ادامه».
5. `success` صفحه `1087:509` (`/checkout/success?order=`): «پرداخت شما با موفقیت انجام شد! / از خرید شما متشکریم»، کارت «مبلغ پرداخت‌شده ۴٬۲۵۰٬۰۰۰ تومان / شناسه تراکنش ۸۷۱۰۹۹۰۹۰۱ / تاریخ ۱۴۰۵/۰۴/۲۸-۱۵:۴۳ / وضعیت: تایید شده»، CTA «پیگیری سفارش / برگشت به فروشگاه».
6. `fail` صفحه `1113:504` (`/checkout/fail`): «عدم موفقیت در پرداخت! / خطا: اختلال در سیستم بانکی! لطفاً کمی دیگر تلاش کنید»، همان کارت با «تایید نشده»، CTA «سفارش دوباره / برگشت به فروشگاه».

### ۴.۶ احراز هویت — ۸ حالت (نشده — P2 همراه چک‌اوت)

شل `app/(auth)/layout.tsx`: کارت وسط + لوگو + تیتر «ورود یا ثبت‌نام در اکسسوری آس». همه ارورها قرمز زیر فیلد.

- `1116:512` ورود: فیلد «شماره موبایل یا ایمیل» + ارور «شماره / ایمیل نامعتبر است!» + CTA «ورود به اکسسوری آس».
- `1154:457` OTP کاربر جدید: «کد تایید را وارد کنید» + «کد به ۰۹۱۹۸۴۲۳۸۱۱ ارسال شد» + تایمر `۰۱:۰۰` + «ارسال مجدد بعد از» + «تایید و ادامه».
- `1168:458` تکمیل ثبت‌نام: نام، موبایل/ایمیل (مقدار علی ملکی)، رمز + تکرار (قانون ۸ حرف، ارور «به‌درستی تکرار بکنید!»).
- `2742:2275` ورود با رمز: فیلد رمز + ارور «رمز عبور اشتباه است!» + لینک‌های «ورود با شماره موبایل / فراموشی رمز عبور».
- `2854:1308` فراموشی ۱: موبایل/ایمیل + ارور نامعتبر.
- `2857:2287` فراموشی ۲: کد به ایمیل `maleki.uix@gmail.com` + تایمر + ارسال مجدد.
- `2860:3265` فراموشی ۳: رمز جدید + تکرار با همان قوانین.
- `2862:4271` فراموشی ۴: «عملیات موفقیت‌آمیز! رمز با موفقیت ویرایش شد» + «برگشت به صفحه ورود».
- پیاده‌سازی: Better Auth (email+password، phoneNumber، emailOTP). روت‌ها: `/login` (سه‌حالته با `?step=`)، `/forgot-password` (`?step=1..4`). محدودیت نرخ ارسال مجدد ۶۰ثانیه سمت سرور.

### ۴.۷ حساب کاربری — ۶ صفحه + ۱۵ مودال (نشده — P3)

شل `app/account/layout.tsx`: سایدبار (نام علی ملکی + ۰۹۱۹۸۴۲۳۸۱۱ + «نمای کلی / سفارش‌ها / علاقه‌مندی‌ها / آدرس‌ها / اطلاعات حساب / خروج») + محتوای راست. بریدکرامب «.../ پروفایل کاربری / داشبورد».

- داشبورد `1999:1384` (`/account`): کارت‌های آمار (فعال ۶ / تحویل‌شده ۳ / مرجوع ۲) + «لیست سفارشات + مشاهده همه» (کارت سفارش: وضعیت بج، کد ۶۸۷۹۱، تاریخ ۱۷ تیر ۱۴۰۵، هزینه).
- سفارش‌ها `2116:694` (`/account/orders?status=`): تب «همه / درحال انجام / تحویل‌شده / مرجوع‌شده» + «۳ سفارش» + لیست کارت‌ها.
- جزئیات `3279:7236` + واریانت `3832:1790` (`/account/orders/[id]`): «جزئیات سفارش + مشاهده فاکتور»، «تاریخ ثبت ۱۴ فروردین ۱۴۰۵ - کد پیگیری ۶۸۷۹۱»، تایم‌لاین ۳ مرحله‌ای (آماده‌سازی/تحویل به پستچی/ارسال به آدرس)، «آخرین بروزرسانی امروز ۱۸:۲۴»، «تحویل ۱ تا ۳ مرداد»، «مبلغ ۴٬۲۵۰٬۰۰۰ / ارسال ۱۵۰٬۰۰۰»، «آدرس ارسالی»، کد رهگیری (`19148010...` یا «در حال آماده‌سازی...» در واریانت ۲)، راهنمای رهگیری پست، CTA «ثبت دیدگاه».
- علاقه‌مندی `2116:5433` (`/account/favorites?sort=`): «۳ کالا» + سورت (جدیدترین/ارزان‌ترین/گران‌ترین) + کارت (بج تخفیف، سایز ۲۴، قیمت، «حذف از علاقه‌مندی‌ها»).
- آدرس‌ها `2124:1141` + ۳ واریانت (`/account/addresses`): «آدرس‌های من + افزودن آدرس جدید»، کارت (متن ولیعصر...، گیرنده، کدپستی ۱۳۱۵۸۱۶۳۱۷، هشدار «موقعیت مکانی مطابقت ندارد»، ویرایش/حذف). مودال `2176:681`: «انتخاب موقعیت مکانی + از روی نقشه / ثبت دستی».
- اطلاعات `2389:8657` (`/account/info`): فیلدها (موبایل تاییدشده + ویرایش، نام علی ملکی، رمز، ایمیل، شبا `IR۱۰۰۰۰۹۴...` حساب مرجوعی). مودال‌ها: ویرایش موبایل ۴ قدم (فرم → OTP → تایید → موفق)، ویرایش ایمیل ۳ قدم، تغییر رمز، کیف مرجوعی ۴ قدم (شبا/تایید/موفق). همه مودال‌ها یک کامپوننت `Modal` + `?modal=` برای deep-link.
- پیاده‌سازی: همه صفحات Server + mutations با Server Actions؛ guard لاگین (redirect `/login?next=`).

### ۴.۸ استاتیک (نشده — P4، کم‌هزینه)

- تماس `2895:5078` (`/contact`): اطلاعات ولیعصر + تلفن‌ها + فرم (نام/موبایل/متن) + ساعات پاسخگویی. `ponytail:` نقشه تصویری ثابت.
- سوالات `3279:4465` (`/faq`): ۸ آکاردئون (زمان ارسال، مرجوعی، پیگیری، ضمانت، روش پرداخت، ارسال سراسری، مشاوره، موجودی). کامپوننت `FaqAccordion` (client) + JSON-LD `FAQPage` برای سئو.
- درباره `3277:1593` (`/about`): «روایتی از سلیقه شما» + پاراگراف بلند فوتر + فیچر ۴تایی.

### ۴.۹ موبایل (~۲۵ فریم — همراه هر صفحه)

- شاپ: هدر جمع‌وجور + سورت/فیلتر به‌صورت باتم‌شیت (`6524:5930`، `6524:7579`).
- محصول: گالری تمام‌عرض + CTA چسبان پایین + مودال «افزوده به سبد» (`6216:4760`).
- اکانت/چک‌اوت: سایدبار → تب‌بار یا آکاردئون؛ جداول → کارت. بریک‌پوینت‌ها: `md:` سایدبار/گرید، `lg:` گرید ۳۳۰px+لیستینگ.

## ۵. معماری بهینه (نهایی)

```
src/app/
  (shop)/layout.tsx            # Header + Footer + CartPopover + MegaMenu
  (shop)/page.tsx              # لندینگ (بعداً) — فعلاً شاپ روی / است
  (shop)/shop/page.tsx         # فروشگاه (هدف نهایی)
  products/[slug]/page.tsx     # ?tab=specs|reviews
  cart/page.tsx                # قدم ۱ (alias /checkout)
  checkout/layout.tsx          # استپر + جزئیات پرداخت
  checkout/address/page.tsx    # حالت خالی/ذخیره‌شده یک روت
  checkout/shipping/page.tsx   # روش ارسال + کوپن
  checkout/success/page.tsx checkout/fail/page.tsx
  payment/callback/route.ts
  (auth)/layout.tsx  login/page.tsx  forgot-password/page.tsx
  account/layout.tsx  account/page.tsx  orders/...  favorites/...  addresses/...  info/...
  contact/page.tsx faq/page.tsx about/page.tsx
  api/health|products|orders|favorites|coupons/route.ts
src/components/shop/          # موجود + CartPopover MegaMenu Gallery Steps Timeline OtpInput AddressCard OrderCard CouponBox QtyStepper Modal BottomSheet FaqAccordion AccountSidebar
src/lib/                       # موجود + coupons.ts shipping.ts orders.ts favorites.ts otp.ts
```

- Server Component دیفالت؛ `use client` فقط: گالری، سبد، فیلتر/سورت، OTP/تایمر، مودال/شیت، آکاردئون، شمارش معکوس.
- کش: لیست/جزئیات `revalidate:60` + `revalidateTag('products')`؛ سبد/سفارش/اکانت `no-store`/`force-dynamic`.
- هر mutation: Server Action + Zod، خروجی `{ok, data?, error?}`، ارور فارسی/لاگ انگلیسی.

## ۶. دیتامدل — دلتای لازم

موجود کافی برای شاپ/ریویو. برای بقیه فیگما اضافه شود:

- `favorites(user_id, product_id, created_at)` pk مرکب.
- `coupons(code unique, pct, max_toman?, min_toman?, active, expires_at?)` — سید `GH632LO`.
- `shipping_methods(slug, title, fee_toman, free_over_toman?)` — سید پست/تیپاکس + رایگان بالای ۵۰۰هزار.
- `orders`: ستون‌های `shipping_slug, shipping_fee_toman, discount_toman, coupon_code?, tracking_code?, status` گسترش به `pending|paid|preparing|shipped|delivered|failed|cancelled|refunded`.
- `order_events(id, order_id, status, created_at)` برای تایم‌لاین.
- `wallet_refunds(user_id, iban, status)` برای کیف مرجوعی. `ponytail:` موجودی کیف واقعی نه — فقط شبا.
- `products.sold_count` برای سورت پرفروش‌ترین (بک‌فیل ۰).
- `addresses`: `label?, recipient, lat?, lng?, is_default`.

## ۷. API و Actions (ترتیب ساخت)

1. سبد: `cart` (Zustand+persist) → `/cart` → اکشن `createOrder` (pending).
2. آدرس CRUD + انتخاب در سفارش (Zod موبایل ۰۹xxxxxxxxx، کدپستی ۱۰رقم).
3. کوپن `validateCoupon` + شیپینگ `setShipping`.
4. پرداخت create/verify (زرین‌پال/زیبال) → success/fail.
5. علاقه‌مندی toggle (خوش‌بینانه)؛ ریویو create (یک‌بار در روز/کاربر).
6. اکانت: سفارش‌ها (فیلتر status)، جزئیات + ایونت‌ها، پروفایل/موبایل OTP/ایمیل/رمز/شبا.

## ۸. پرداخت ایرانی (sandbox)

- `POST /api/checkout` → pending + `amount_rial = (total - discount + shipping) * 10` → redirect درگاه.
- callback → verify → paid: کسر موجودی، `sold_count += qty`، ثبت ایونت، `revalidateTag('products')`، redirect success؛ خطا → fail با پیام بانکی فیگما.
- بدون merchant واقعی = mock authority در dev (فایل جدا).

## ۹. فازبندی اجرایی

- **P2 (بعدی):** سبد کامل (`/cart` + پاپ‌اور) → چک‌اوت ۶ قدم → پرداخت mock → success/fail. سپس لاگین ۸ حالت (پیش‌نیاز آدرس/سفارش واقعی).
- **P3:** اکانت ۶ صفحه + مودال‌ها → علاقه‌مندی → مگامنو → مودال ثبت‌نظر واقعی.
- **P4:** لندینگ `/` + انتقال شاپ به `/shop` → تماس/سوالات/درباره → موبایل‌شیت‌ها → سئو/متا fa/سایت‌مپ/PWA/Sentry/ریت‌لیمیت → دیپلوی VPS.
- هر فاز: `typecheck + test + build` سبز → کامیت+پوش `origin main` + گزارش هش.

## ۱۰. قراردادها و Definition of Done

- تومان همه‌جا، ریال فقط درگاه؛ اعداد فارسی؛ ارور فارسی.
- `Doran FaNum ONLY` — هیچ فونت دیگری نه inline نه fallback.
- باگ‌های فیگما (فیکس در کد): ۳ لیبل TELEGRAM تکراری → آیکون‌های واقعی؛ ۵ فریم هم‌نام Price range؛ دیتای placeholder تکراری (۴۸۳ دیدگاه، قیمت ۴٬۲۵۰٬۰۰۰) → دیتای واقعی؛ `U+2028` در AS accessory؛ تایپوها (کسسوری→اکسسوری، Abous→About).
- DoD هر صفحه: دسکتاپ پیکسل‌نزدیک فیگما + موبایل بدون اسکرول افقی + کیبورد/ARIA مودال‌ها + empty/error/loading + تست Vitest برای منطق + بیلد سبز.
